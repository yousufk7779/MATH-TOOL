const { chapterContents } = require('./client/data/chapterContent');
console.log("Successfully loaded chapterContents");
console.log("Total chapters registered:", Object.keys(chapterContents).length);
